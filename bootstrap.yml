---
# Bootstrap compute instances into a standard base state

 - hosts: bootstrap
   user:  root

   vars:
    ansible_ssh_port: 2222
    ansible_ssh_user: root

   roles:
    - bootstrap
    - users


# Next play in the playbook
# - hosts: users
#   user: root
#
#   vars:
#    ansible_ssh_port: 2222
#    ansible_ssh_user: root
#
#   roles:
#    - users  
   

